import React from 'react'

import { Button, Container, Row, Col } from 'reactstrap'

const ProductDetail = ({ }) => {
    return (
        <div>
            
        </div>
    );
}

export default ProductDetail;